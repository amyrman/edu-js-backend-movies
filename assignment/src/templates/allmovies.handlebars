<ul>

  {{!-- Loop through movies data (id and title) fetched through API (in movies.js), with handlebars built-in #each-helper, and insert data --}}
{{#each movies }}
  <li>
    <a href="/movies/{{ this.id }}">
      {{ this.title }}
    </a>
  </li>
{{/each}}

</ul>
